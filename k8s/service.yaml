apiVersion: v1
kind: Service
metadata:
  name: i27-helpdesk-gateway
  namespace: ${NAMESPACE}
spec:
  selector:
    app: i27-helpdesk-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort